# User workspace and unsaved files
.user/*
.unsaved/*
.cache/*

# Build Folder. Output of banks if not explicitly set in the project settings
# Can be generated by user
Build/*

# Always include Metadata and Assets
!Metadata/*
!Assets/*